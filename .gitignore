# Python
__pycache__/
*.pyc
.env
.venv/

# Node
node_modules/
dist/
*.tsbuildinfo

# Satellite data
*.tif
*.geotiff
*.nc
data/

# ML weights — except the one the backend loads, which ships in the image.
*.pt
!backend/models/*.pt

# DB
*.db

# Scene transfer export (imagery-derived, ~7 MB, not for the repo)
backend/export.jsonl.gz

# Claude Code
.claude/settings.local.json
.claude/worktrees/
.claude/plans/
docs/superpowers/specs/
docs/superpowers/plans/
.worktrees/
