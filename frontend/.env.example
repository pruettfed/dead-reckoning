# Vite dev server proxy target — native dev only.
VITE_API_PROXY_TARGET=http://localhost:8000

# Analysis is scheduled server-side; the frontend has no trigger and needs no key.
# The admin endpoints are reachable with curl and ANALYSIS_API_KEY when ops needs
# to force a run.
