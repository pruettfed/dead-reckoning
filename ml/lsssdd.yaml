# Ultralytics dataset config for the converted LS-SSDD-v1.0 layout (see prepare_dataset.py).
# `path` is resolved against the ultralytics `datasets_dir` setting — the runbook
# sets it with:  yolo settings datasets_dir=/content/datasets
path: lsssdd
train: images/train
val: images/val
names:
  0: ship
